---
description: Frontend development standards for Next.js React TypeScript stack
alwaysApply: false
---
# 📱 Frontend Stack Standards

## Framework & Libraries
- Next.js 15.3.3 (App Router) + React 19.1 + TypeScript 5.x (strict)
- TanStack Query 5.80.7 + Zustand 4.5.2
- Tailwind CSS 3.4.1 + shadcn/ui + Lucide-React  
- NextAuth.js 5.0.0-beta.28

## Critical Rules
1. **Never** hard-code backend URL—use `process.env.NEXT_PUBLIC_BACKEND_URL`
2. Use helper in `lib/api.ts`; SSE via browser streams
3. Components: `PascalCase.tsx`; hooks: `useX.ts`; utils: `camelCase.ts`
4. Always use Next.js `<Image />` component with width/height
5. Touch targets minimum 44x44px (workers wear gloves)

## API Integration Pattern
```typescript
import { api } from '@/lib/api';
import { useQuery } from '@tanstack/react-query';

export function useData() {
  return useQuery({
    queryKey: ['data'],
    queryFn: () => api.get('/api/endpoint'),
    staleTime: 5 * 60 * 1000,
    retry: (failureCount, error) => {
      if (error instanceof APIError && error.status === 401) return false;
      return failureCount < 3;
    },
    networkMode: 'offlineFirst',
  });
}
```
